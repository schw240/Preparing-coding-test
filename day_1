#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	int x, y;
	int value = 0;

	scanf("%d %d", &x, &y);

	if (x > y) {
		printf("x가 큽니다.\n");
		value = x;
	}
	else if (x < y){
		printf("y가 큽니다.\n");
		value = y;
	}
	else {
		printf("같습니다.\n");
		value = x;
	}

	printf("%d\n", value);

	return 0;
}
